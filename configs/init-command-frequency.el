;; From http://nflath.com/2009/08/command-frequency-mode/
;; and http://xahlee.org/emacs/command-frequency.html
;; Seems to be blowing up now
;; (setq-default command-frequency-table-file "~/.emacs.d/frequencies")
;; (require 'command-frequency)
;; (command-frequency-table-load)
;; (command-frequency-mode 1)
;; (command-frequency-autosave-mode 1)

(provide 'init-command-frequency)
